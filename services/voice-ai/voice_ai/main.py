"""Voice AI — entry point (placeholder)."""
import logging
logger = logging.getLogger("greg.voice-ai")

def main():
    logger.info("Voice AI service — pas encore implémenté.")
    logger.info("Ce service gérera STT → LLM → TTS pour la conversation vocale.")

if __name__ == "__main__":
    main()
